<%= form_for(@zologico) do |f| %>
  <% if @zologico.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@zologico.errors.count, "error") %> prohibited this zologico from being saved:</h2>

      <ul>
      <% @zologico.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
